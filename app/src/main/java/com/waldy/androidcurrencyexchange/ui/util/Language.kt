package com.waldy.androidcurrencyexchange.ui.util

/**
 * Represents the languages supported by the application.
 * Each language has a unique tag (e.g., "en", "vi") that can be used for locale settings.
 */
enum class Language(val tag: String) {
    ENGLISH("en"),
    VIETNAMESE("vi")
}
